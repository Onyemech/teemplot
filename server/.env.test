# Test Environment Configuration
# This file is used for running property-based tests and integration tests

NODE_ENV=test

# Test Database (using Supabase with direct connection to bypass pooler for session variables)
# Using port 5432 (direct) instead of 6543 (pooler) for proper session variable support
DATABASE_URL=postgresql://postgres:Teemplot123$@db.hhynzmnijsqbkuvlfisq.supabase.co:5432/postgres

# JWT Secret (test only)
JWT_SECRET=test_jwt_secret_key_for_property_tests_only

# Session Secret (test only)
SESSION_SECRET=test_session_secret_key_for_property_tests_only

# Disable external services in tests
SKIP_EMAIL_SENDING=true
SKIP_EXTERNAL_API_CALLS=true

# Google Auth (Test Mocks)
GOOGLE_CLIENT_ID=test_google_client_id
GOOGLE_CLIENT_SECRET=test_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Test-specific settings
TEST_TIMEOUT=60000
PROPERTY_TEST_ITERATIONS=100
